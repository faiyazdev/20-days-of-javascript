# Day 07 — Questions

## The `this` Keyword in JavaScript

---

### 🔵 Understanding `this`

1. What is the `this` keyword in JavaScript?
2. What does `this` refer to in the global scope?
3. What does implicit binding of `this` mean?
4. What does a standalone function mean?
5. What is the behaviour of the `this` keyword inside of a standalone function?
6. What is the behaviour of the `this` keyword inside of an arrow function?

---

### 🔵 Strict Mode & `this`

7. What effects do you think `this` can bring if it is used inside of a standalone function in strict mode?
8. What about arrow functions — does `this` refer to `undefined` in strict mode?

---

### 🔵 Explicit Binding

9. What is explicit binding?
10. What is the `call` method?
11. What is `apply`?
12. What is `bind`?
13. What is the difference between `call`, `apply`, and `bind`? How do they relate to explicit binding?

---

### 🔵 `new` & Overview

14. How does the `new` keyword in a constructor function relate to `this`?
15. Quick overview of the `this` keyword?

---

### 🔵 Interview Questions

16. Common interview questions and answers on the `this` keyword?

---

### 🔴 Part 02 — Errors & Error Handling

17. What are errors in JavaScript and how do you handle them with `try-catch`?
18. What is the Error object?
19. What does throwing an error mean?
20. What is rethrowing an error? (detailed)
21. What is the use case of `finally` in `try-catch`?
22. Is `Error` a constructor function?
23. How do you create custom errors using the built-in `Error` constructor function?
